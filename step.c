#include <stdio.h>
#include <string.h>
#define MAX 10000

void step(char c){
    FILE *fp;
    char str[MAX];
    char x[MAX];
    int i =0;

	char in[2];
	int j = 0;
	while(1){
		scanf("%s",in);
	}  

   fclose(fp);

  return 0;
}
